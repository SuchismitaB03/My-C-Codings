#include <iostream>
using namespace std;
int main ()
{
int max = 1000, min = 10;
cout << (( max > min ) && ( max < min));
cout << (( max > min ) || ( max < min ));
cout << ( !1);

    return 0;
}